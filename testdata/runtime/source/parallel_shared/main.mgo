package main

func Main() int {
	guard := make(chan int, 1)
	done := make(chan int, 4)
	guard <- 1
	total := 0
	for i := 0; i < 4; i++ {
		go func() {
			<-guard
			total++
			guard <- 1
			done <- 1
		}()
	}
	for i := 0; i < 4; i++ {
		<-done
	}
	return total
}
